function sockMerchant(n, ar) {
    let pair = 0
    let val = new Set()
    ar.forEach(i => {
        val.add(i)
    })
    for (i of val) {
        let num = ar.filter(item => item == i).length
        pair += parseInt(num / 2)
    }
    return pair
}
